import React from 'react';
const SubmitListItemButton = ({text}) => {
  return (
    <button 
      className="btn-submit"
    >
      {text}
    </button>
  );
};
export default SubmitListItemButton;